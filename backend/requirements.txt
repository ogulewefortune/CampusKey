# Python dependencies for CampusKey backend
# This file lists all Python packages required for the backend server
# Install with: pip install -r requirements.txt

# Core API
# FastAPI - modern, fast web framework for building APIs with Python
fastapi==0.104.1
# Uvicorn - ASGI server to run FastAPI application (handles HTTP requests)
uvicorn==0.24.0
# python-multipart - required for handling file uploads and form data in FastAPI
python-multipart==0.0.6

# Biometric Authentication
# opencv-python - OpenCV library for computer vision and image processing
# Used for face detection and image manipulation
opencv-python==4.8.1.78
# face-recognition - Python library built on dlib for face recognition
# Provides easy-to-use face recognition API
face-recognition==1.3.0
# dlib - C++ library with Python bindings for machine learning
# Required by face-recognition for face detection and encoding
dlib==19.24.0
# numpy - fundamental package for numerical computing in Python
# Required by OpenCV and face-recognition for array operations
numpy==1.24.3
# Pillow - Python Imaging Library for image processing
# Used for loading, saving, and manipulating image files
Pillow==10.0.0

# Security & Database
# pyjwt - JSON Web Token library for creating and verifying JWT tokens
# Used for secure authentication tokens between mobile app and backend
pyjwt==2.8.0
# bcrypt - password hashing library
# Used for securely storing and verifying user passwords
bcrypt==4.0.1
# python-decouple - library for managing environment variables
# Used for loading configuration from .env file securely
python-decouple==3.8
# Note: sqlite3 is included in Python standard library, no need to install
# SQLite3 comes built-in with Python for lightweight database operations

# Email (for password recovery)
# Note: smtplib is included in Python standard library, no need to install
# smtplib - used for sending emails via SMTP protocol
# email-validator - validates email addresses format
email-validator==2.0.0

# Utilities
# Note: The following are built-in Python modules and don't need to be installed:
# - secrets: Generate cryptographically strong random numbers
# - string: String constants and utilities
# - datetime: Date and time manipulation
# - random: Generate pseudo-random numbers

# RFID simulation and NFC (optional - for hardware integration)
# pyserial - library for serial port communication
# Used for communicating with RFID/NFC hardware readers via serial port
pyserial==3.5
# nfcpy - Python library for Near Field Communication (NFC)
# Used for reading/writing NFC tags and cards
nfcpy==1.0.4
# pyusb - Python library for USB device access
# Used for communicating with USB-based RFID/NFC readers
pyusb==1.2.1
